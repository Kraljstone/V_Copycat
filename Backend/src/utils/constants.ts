export enum Routes {
  USERS = 'users',
}

export enum Services {
  USERS = 'USERS_SERVICE',
  USERS_REPOSITORY = 'USERS_REPOSITORY',
}

export enum Tables {
  USERS = 'users',
}

export enum Database {
  DATA_SOURCE = 'DATA_SOURCE',
}
